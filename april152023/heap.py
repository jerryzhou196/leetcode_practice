def deleteMax(heap_arr):
    last = heap_arr[len(heap_arr) - 1]
    while parent(last):

def fixup(heap_arr, i):
    # insert element at the first free leaf
    while (parent(i) and A[parent(i)].key < A[i].key):
        # swap A[i] and A[parent(i)]
    
