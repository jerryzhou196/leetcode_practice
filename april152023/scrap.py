from typing import List


alphabet = [a,b,c,d,e...]
# string is defined to be a sequence
# x1, x2, ... len(word) where x_i in alphabet
# len(word) = n > 0
word: List[alphabet] = []

# one str only consists of {0,1,2}
# len(T) = t
T: List[word[0,1,2]]

# S is a list of tuples of length t such that
# x1 + x2 + ... + xt = n
# xi <= 2
# w(M) of a multiset M is it's size
S: List[Tuple[int, ...]]

# define a bijection between set S and set T
